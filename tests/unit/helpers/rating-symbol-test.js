import { ratingSymbol } from 'book-review/helpers/rating-symbol';
import { module, test } from 'qunit';

module('Unit | Helper | rating symbol');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingSymbol([42]);
  assert.ok(result);
});
